/**
 * Tower of Hanoi - Main Initialization
 * Entry point that bootstraps the game
 */

// Start the game when DOM content is loaded
document.addEventListener('DOMContentLoaded', function() {
    console.log('Tower of Hanoi game initializing...');

    // Initialize game
    initGame();
});